#include<stdio.h>
#include<string.h>
int main()
{
    char name[100];
    gets(name); // it takes input with spaces untill we hit the "enter"
    // scanf("%s",name); // it takes input untill it finds a blanket space
    // puts(name);
    printf("%s",name);
    printf("%s",name);
    printf("%s",name);
    printf("%s",name);
}