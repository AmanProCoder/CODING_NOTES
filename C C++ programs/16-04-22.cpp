#include<iostream>
#include<string>
#include<cstring>
#include<math.h>
using namespace std;
int main()
{
    //bool_ =C99 (in C) 
    //int , double,float , char, boolean 
    //bool a; true or false
    cout<<"welcome to cpp language"; //console output <<-insertion operator :: resolution operator 
    int a;
    // cin>>a; // >> extraction operator, console input 
    // cout<< "the entered no is:"<<a;

    // //in c - condition result is - non zero(true) or zero(false)
    // //in CPP- only true or false - 0 or 1 
    // a=9;
    // if(a=0)
    // cout<<"true "<<a;
    // else
    // cout<<"false "<<a;

    // cout<<"\noutside"<<a;
    // return 0;

    //in cpp switch also accepts the boolean values
    char n[90]="maan",m[89]="aman";
    a=strcmp(m,n);
    a=pow(9,2);
}